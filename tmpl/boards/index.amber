html
  head
    title #{Title}
  body
    h3 Boards
    import form
    div#content
      each $item in Boards
        p
          a[href="boards/" + $item.ID]
            b
              #{$item.Title}
        p #{$item.Body}
