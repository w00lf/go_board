html
  head
    title #{Title}
  body
    h1 #{Board.Title}
    import form
    div#content
      each $item in Posts
        p
          a[href="/boards/" + $item.BoardID + "/posts/" + $item.ID]
            #{$item.CreatedAt}
        p #{$item.Body}
